import assert from "node:assert/strict"; import test from "node:test"; import { enrichExtractedFrames } from "./frame-telemetry-enrichment.js";
test("matches telemetry and tolerates EXIF failure",async()=>{const sample={timestampMs:0,capturedAt:new Date(),latitude:31,longitude:121,relativeAltitudeMeters:10,absoluteAltitudeMeters:20,gimbalYawDegrees:1,gimbalPitchDegrees:-30,gimbalRollDegrees:0,focalLengthMillimeters:40,digitalZoomRatio:1}; const result=await enrichExtractedFrames("v.mp4",[{fileName:"f.jpg",storagePath:"/f.jpg",fileSize:1,timestampMs:0}],{extractVideoTelemetry:async()=>({source:"dji_subtitle",samples:[sample],warnings:[]}),writeFrameExif:async()=>{throw new Error("x")}}); assert.equal(result.frames[0].telemetry?.latitude,31); assert.equal(result.stats.exifFailedFrameCount,1);});
