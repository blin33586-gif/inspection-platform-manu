import assert from "node:assert/strict"; import test from "node:test"; import { buildFrameExifTags } from "./frame-exif-writer.js";
test("builds GPS time altitude and focal tags",()=>{const tags=buildFrameExifTags({timestampMs:0,matchOffsetMs:0,capturedAt:new Date("2026-07-08T08:12:57.018Z"),latitude:31.229247,longitude:121.634603,relativeAltitudeMeters:79.969,absoluteAltitudeMeters:96.474,gimbalYawDegrees:-67.6,gimbalPitchDegrees:-30,gimbalRollDegrees:0,focalLengthMillimeters:40,digitalZoomRatio:1}); assert.equal(tags.GPSLatitude,31.229247); assert.equal(tags.GPSAltitude,96.474); assert.equal(tags.FocalLength,40);});
