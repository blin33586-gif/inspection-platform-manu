export function buildTencentMapUrl(latitude: number | null, longitude: number | null, title: string) { if (latitude === null || longitude === null) return null; const marker = encodeURIComponent(`coord:${latitude},${longitude};title:${title};addr:${title}`); return `https://apis.map.qq.com/uri/v1/marker?marker=${marker}&referer=xunjianbao`; }
export function buildTencentNavigationUrl(latitude: number | null, longitude: number | null, title: string) { if (latitude === null || longitude === null) return null; return `https://apis.map.qq.com/uri/v1/routeplan?type=drive&to=${encodeURIComponent(title)}&tocoord=${latitude},${longitude}&policy=0&referer=xunjianbao`; }
